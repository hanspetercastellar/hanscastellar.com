services:
  web:
    image: ghcr.io/hancel1512/hanscastellar:latest
    container_name: hanscastellar
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
